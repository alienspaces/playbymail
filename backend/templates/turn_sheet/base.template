<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{if .GameName}}{{.GameName}}{{else}}Turn Sheet{{end}} - Turn {{if .TurnNumber}}{{.TurnNumber}}{{else}}0{{end}}</title>
    <style>
        :root {
            --page-width: 210mm;
            --page-height: 297mm;
            --padding-vertical: 8mm;
            --padding-horizontal: 18mm;
            --content-container-width: 168mm; /* 80% of A4 width (210mm * 0.8) */
            --content-container-margin-horizontal: 21mm; /* 10% margin on each side */
            --content-container-margin-vertical: 6mm; /* Small space at top and bottom */
        }

        /* Debug - set to transparent to show background images */
        .debug-frame {
            background-color: transparent;
        }

        .debug-main {
            background-color: transparent;
        }

        .debug-section {
            background-color: transparent;
        }

        /* Base styles for PDF generation */
        body {
            font-family: 'Times New Roman', serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            color: #333;
            display: flex;
            justify-content: center;
            print-color-adjust: exact;
            -webkit-print-color-adjust: exact;
        }
        
        h1 {
            margin-top: 2mm;
            margin-bottom: 2mm;
        }

        h2 {
            margin-top: 2mm;
            margin-bottom: 2mm;
        }
        
        h3 {
            margin-top: 2mm;
            margin-bottom: 2mm;
        }
        
        /* Page wrapper contains both background and content */
        .page-wrapper {
            position: relative;
            width: var(--page-width);
            height: var(--page-height);
            overflow: hidden;
        }
        
        /* Single background image covering the page */
        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--page-height);
            object-fit: cover;
            object-position: center;
            pointer-events: none;
            z-index: 0;
        }

        .content {
            position: relative;
            z-index: 2;
            width: 100%;
        }

        /* Main container for all turn sheet content sections */
        .content-container {
            width: var(--content-container-width);
            margin: var(--content-container-margin-vertical) var(--content-container-margin-horizontal);
            box-sizing: border-box;
        }

        /* Common container for all content sections */
        .content-section {
            margin: 0 0 6mm 0;
            padding: 2mm;
            box-sizing: border-box;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.75);
            width: 100%;
        }

        .header-content {
            text-align: center;
            min-height: 46mm;
            background-color: rgba(255, 255, 255, 0.75);
        }
        
        .header-content h1 {
            color: #2c3e50;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.8), -1px -1px 2px rgba(255, 255, 255, 0.8);
        }
        
        .header-content h2 {
            color: #7f8c8d;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.8);
        }

        .header-content-title {
            text-align: center;
        }

        .header-content-title h3 {
            color: #2c3e50;
            font-weight: 600;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.8), -1px -1px 2px rgba(255, 255, 255, 0.8);
        }

        .header-content-title p {
            margin: 0;
            color: #555;
            line-height: 1.4;
            max-width: 560px;
            margin-left: auto;
            margin-right: auto;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.8);
        }


        .instruction-content {
            min-height: 10mm;
            background-color: rgba(255, 255, 255, 0.8);
            border-left: 3px solid #546a7b;
            color: #2c3e50;
        }

        .instruction-content--empty {
            background: transparent;
            border: none;
        }

        .instruction-content p {
            margin: 0;
        }

        .instruction-content strong {
            color: #1f2a36;
        }

        .main-content {
            min-height: 140mm;
            overflow: visible;
        }

        /* Turn sheet content is contained within the main content*/
        .main-content label,
        .main-content .form-field-title {
            display: block;
            font-size: 14px;
            color: #2c3e50;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .main-content .form-field-description {
            display: block;
            font-size: 12px;
            color: #6b7280;
            margin-top: 2px;
        }

        .main-content input[type="text"],
        .main-content input[type="email"],
        .main-content input[type="number"],
        .main-content select,
        .main-content textarea {
            width: 100%;
            padding: 6px 10px;
            border: 1px solid #d1d5db;
            border-radius: 4px;
            font-size: 12px;
            color: #2c3e50;
            box-sizing: border-box;
        }

        .main-content input[type="text"]:focus,
        .main-content input[type="email"]:focus,
        .main-content input[type="number"]:focus,
        .main-content select:focus,
        .main-content textarea:focus {
            outline: none;
            border-color: #6b7280;
            box-shadow: 0 0 0 1px rgba(107, 114, 128, 0.1);
        }
        
        .turn-sheet-info {
            margin-top: 15px;
            padding: 10px;
            background-color: transparent;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 10px;
            word-break: break-all;
        }
        
        .turn-sheet-info-label {
            font-weight: bold;
            margin-bottom: 5px;
            color: #495057;
        }
        
        .turn-sheet-info-value {
            color: #6c757d;
        }

        .footer-content {
            position: absolute;
            bottom: var(--content-container-margin-vertical);
            left: 50%;
            transform: translateX(-50%);
            width: var(--content-container-width);
            margin: 0;
            padding: 4mm 2mm;
            background-color: rgba(255, 255, 255, 0.7);
            border-top: 1px solid #dee2e6;
            border-radius: 8px;
            z-index: 3;
            box-sizing: border-box;
        }

        .footer-content-item {
            padding: 1mm 0;
            border-bottom: 1px solid #dee2e6;
            font-family: 'Courier New', monospace;
            font-size: 10px;
            word-break: break-all;
        }

        .footer-content-item:last-child {
            border-bottom: none;
        }

        .footer-content-label {
            font-weight: bold;
            margin-bottom: 3px;
            color: #495057;
        }
        
        .footer-content-value {
            color: #6c757d;
        }

        .main-content .section-title,
        .main-content .subsection-title,
        .main-content .form-section-title,
        .main-content .content-section-title {
            text-align: center;
            font-size: 16px;
            font-weight: 600;
            color: #2c3e50;
            margin: 14px 0 10px 0;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.9), -1px -1px 2px rgba(255, 255, 255, 0.9);
        }

        .main-content .section-subtitle,
        .main-content .content-section-subtitle {
            text-align: center;
            font-size: 14px;
            color: #555;
            margin: 0 0 12px 0;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.9);
        }
        
        /* Form styling */
        .form-group {
            margin-bottom: 15px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #2c3e50;
        }
        
        .form-group input[type="text"],
        .form-group input[type="email"] {
            width: 100%;
            padding: 8px 12px;
            border: 1px solid #bdc3c7;
            border-radius: 4px;
            font-family: 'Times New Roman', serif;
            font-size: 14px;
            background-color: white;
        }
        
        .form-group input[type="text"]:focus,
        .form-group input[type="email"]:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 5px rgba(52, 152, 219, 0.3);
        }
        
        @media print {
            body {
                background-color: white;
            }
            
            /* Keep background image visible in print/PDF mode */
            .background-image {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
            
            .content {
                background-color: transparent;
                box-shadow: none;
                margin: 0;
                padding: 0;
            }
        }
    </style>
    
    <!-- Child template styles -->
    {{block "styles" .}}{{end}}
</head>
<body>
    <!-- Page wrapper contains both background and content -->
    <div class="page-wrapper">
        <!-- Background image -->
        {{if .BackgroundImage}}
        <img class="background-image" src="{{.BackgroundImage | safeURL}}" alt="" />
        {{end}}

        <!-- Main content -->
        <div class="content debug-frame">
            <!-- Container for all turn sheet content sections -->
            <div class="content-container">
                <!-- Header content -->
                <div class="content-section header-content">
                    <h1>{{if .GameName}}{{.GameName}}{{else}}Game{{end}}</h1>
                    <h2>Turn {{if .TurnNumber}}{{.TurnNumber}}{{else}}0{{end}}{{if .AccountName}} - {{.AccountName}}{{end}}</h2>

                    {{if or .TurnSheetTitle .TurnSheetDescription}}
                    <div class="header-content-title">
                        {{if .TurnSheetTitle}}<h3>{{.TurnSheetTitle}}</h3>{{end}}
                        {{if .TurnSheetDescription}}<p>{{.TurnSheetDescription}}</p>{{end}}
                    </div>
                    {{end}}
                </div>

                <!-- Instruction content -->
                {{if .TurnSheetInstructions}}
                <div class="content-section instruction-content">
                    <p><strong>Instructions:</strong> {{.TurnSheetInstructions}}</p>
                </div>
                {{else}}
                <div class="content-section instruction-content instruction-content--empty"></div>
                {{end}}

                <!-- Main content area -->
                <div class="content-section main-content debug-main">
                    {{template "content" .}}
                </div>
            </div>
        </div>

        <!-- Fixed footer content at bottom of page -->
        {{if or .TurnSheetDeadline .TurnSheetCode}}
        <div class="footer-content">
            <!-- Turn Sheet Deadline -->
            {{if .TurnSheetDeadline}}
            <div class="footer-content-item">
                <div class="footer-content-label">Turn Sheet Deadline:</div>
                <div class="footer-content-value">{{.TurnSheetDeadline}}</div>
            </div>
            {{end}}

            <!-- Turn Sheet Identification Code -->
            {{if .TurnSheetCode}}
            <div class="footer-content-item">
                <div class="footer-content-label">Turn Sheet Code:</div>
                <div class="footer-content-value">{{.TurnSheetCode}}</div>
            </div>
            {{end}}
        </div>
        {{end}}
    </div>
</body>
</html>
